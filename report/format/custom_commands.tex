% Custom LaTeX Commands
% Reusable commands for tables, figures, and formatting

% Ensure consistent font usage throughout document
% Times font is set as default via \usepackage{times}
% Inconsolata is used for \texttt{} commands (monospace)
\renewcommand{\familydefault}{\rmdefault}

% Example: Command for consistent table formatting
% \newcommand{\tableheader}[1]{\textbf{#1}}

% Add custom commands here as needed
